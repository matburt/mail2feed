# Database configuration
DATABASE_URL=sqlite://mail2feed.db

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=3001

# Logging level (error, warn, info, debug, trace)
RUST_LOG=info

# CORS settings (for development, use * for all origins)
CORS_ALLOWED_ORIGINS=*